// Jib (집세일) — forestay 기반 삼각형. 미구현 stub.
// 다음 단계에서 buildShape / buildGeometry 구현 예정.

export const meta = {
  key: 'jib',
  label: 'Jib',
  slot: 'headsail',
  trimKeys: ['halyard', 'sheet', 'jibLead', 'inhauler'],
  defaults: { halyard: 0.60, sheet: 0.55, jibLead: 0.50, inhauler: 0.20 },
  implemented: false,
};

export function buildShape() { return null; }
export function buildGeometry() { return null; }
